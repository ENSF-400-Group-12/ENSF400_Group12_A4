# Server
PORT=8080

# Session (use a long random string in production)
SESSION_SECRET=change-me-in-production

# CORS: frontend origin for cookie-based auth (local dev)
FRONTEND_ORIGIN=http://localhost:3000
